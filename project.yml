name: Eyethu
options:
  bundleIdPrefix: app.eyethu
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "15.0"
targets:
  Eyethu:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: Eyethu
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.eyethu.ios
        DEVELOPMENT_TEAM: ""
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_FILE: Eyethu/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
